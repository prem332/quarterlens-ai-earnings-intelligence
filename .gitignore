# ---- Secrets / env ----
.env
.env.*
!.env.example

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
dist/
build/

# ---- Data pipeline artifacts (regenerable, large) ----
data/

# ---- IDE / OS ----
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ---- Test / coverage ----
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

# ---- Logs ----
*.log
k8s/bin/

# ---- MLflow local tracking store ----
# Regenerable local experiment data. Was committed until 2026-08-02 -- caused
# a real bug: a Windows-absolute artifact_location baked into the committed
# mlflow.db crashed CI on the Linux runner (see CLAUDE.md's "MLOps / Deploy
# Gate" section). Locked baseline numbers live in CLAUDE.md/evaluation/
# FINAL_REPORT.md, not in this file, so nothing of record is lost by not
# tracking it.
mlflow.db
mlruns/
