--extra-index-url https://download.pytorch.org/whl/cpu

# Direct dependencies only — transitive packages are left for pip to resolve.
# (A prior version of this file hard-pinned ~90 transitive packages to one
# venv's drifted snapshot; several were mutually inconsistent — e.g. a pinned
# `cryptography==49.0.0` while mlflow requires `<49` — which is unresolvable
# by construction regardless of what pip tries. Direct-deps-only avoids that
# class of problem and lets each library's own declared constraints govern its
# own dependency tree.)

# Web framework / server
fastapi==0.139.0
uvicorn==0.51.0
pydantic<2.13,>=2.11.9          # crewai 1.15.3 requires pydantic<2.13

# Orchestration
langgraph==1.2.9
crewai==1.15.3

# Azure SDKs
azure-core==1.41.0
azure-cosmos==4.16.1
azure-identity==1.25.3
azure-keyvault-secrets==4.11.0
azure-monitor-opentelemetry          # NOT pinned: 1.8.9 (what's locally installed) requires
                                      # opentelemetry-sdk~=1.43.0, which conflicts with
                                      # crewai 1.15.3's opentelemetry-sdk~=1.42.0 -- confirmed via
                                      # a real fresh-install failure (2026-08-02). Unpinned lets
                                      # pip's resolver pick a version compatible with crewai's
                                      # constraint, same as before this was ever pinned.
azure-search-documents==12.0.0
azure-storage-blob==12.30.0
cryptography<49,>=43.0.0        # mlflow 3.14.0 requires cryptography<49

# LLM / embeddings / ML
openai==2.45.0
tiktoken==0.13.0
torch==2.13.0+cpu
transformers==5.13.1
sentence-transformers==5.6.0
numpy==2.2.6

# Data pipeline
beautifulsoup4==4.15.0
lxml==6.1.1
pyodbc==5.3.0
PyYAML==6.0.3
requests==2.34.2

# Caching / storage
redis==8.0.1

# Report generation
python-docx==1.2.0
reportlab==5.0.0

# Observability
mlflow==3.14.0
langfuse==4.14.0
arize-phoenix-otel==0.16.1
openinference-instrumentation-openai==0.1.52
openinference-instrumentation-langchain==0.1.67

# Config
python-dotenv==1.2.2
typing_extensions==4.16.0

# Testing (CI)
pytest==8.4.2
pytest-asyncio==0.25.2
